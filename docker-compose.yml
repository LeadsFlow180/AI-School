services:
  allen-girls-adventure:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      # Optional: mount server-providers.yml for provider config
      # - ./server-providers.yml:/app/server-providers.yml:ro
      - allen-girls-adventure-data:/app/data
    restart: unless-stopped

volumes:
  allen-girls-adventure-data:
